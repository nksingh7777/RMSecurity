type Client{
    address: String
    clientName: String
    ClientID: String
    gst: String
}

type ClientData{
    clientId: Int
    name: String
    address: String
    gst: String
    active: Boolean
}

type Query{
    getClient(id: Int): Client
    getClientData(id: Int): ClientData

}